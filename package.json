{
  "name": "afc-football-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Application full-stack de gestion d'une équipe de football (thème terrain de foot).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && npm run prisma:generate --prefix server",
    "start": "npm run start --prefix server",
    "prisma:migrate": "npm run prisma:deploy --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
